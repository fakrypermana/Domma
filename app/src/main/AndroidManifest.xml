<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.letbemagi.magi.domma">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Tab.Spalsh">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <activity android:name=".Activity.MainActivity" />
        <activity android:name=".Activity.InputTransIncomeActivity" />
        <activity android:name=".Activity.InputTransOutcomeActivity" />
        <activity android:name=".Activity.ChartActivity" />
        <activity android:name=".Activity.ChartIncomeActivity" />
        <activity android:name=".Activity.ChartOutcomeActivity" />
        <activity android:name=".Activity.LoginActivity" />
        <activity android:name=".Activity.RegistrationActivity"></activity>
        <activity android:name=".Activity.ReportActivity" />
    </application>

</manifest>